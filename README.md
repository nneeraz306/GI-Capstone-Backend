run main.py (runs normalization_l1 and normalization_l2) for each bdc (3)
OUTPUT-> consolidated and normalized csv for each of the BDC (3)

run mapping.py (gives desired output fields)
OUTPUT->mapped_portfolio_data.csv (final deliverable)